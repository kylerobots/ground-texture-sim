name: Unit Tests

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Build the Docker image
      run: docker build . --target test --file Dockerfile --tag ground-texture-sim:test
    - name: Run tests
      run: docker run ground-texture-sim:test ctest -VV
